// Toggle to false to temporarily disable resume downloads (ex. while the resume is out of date).
// When disabled, the resume button shows a toast instead of downloading the file.
export const FEATURES = {
  resumeDownloadEnabled: false,
};
